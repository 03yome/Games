-- FIXZ HUB | DRAW PERFECT CIRCLE 📍
-- Using Fluent UI Library

print("📍 Loading Fixz Hub for Draw Perfect Circle...")

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local VirtualUser = game:GetService("VirtualUser")

local plr = Players.LocalPlayer

-- Anti-AFK
plr.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

-- Load Fluent UI
print("Loading Fluent UI...")
local success, Fluent = pcall(function()
    return loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
end)

if not success then
    warn("Failed to load Fluent UI, trying alternate link...")
    Fluent = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Fluent.lua"))()
end

local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

print("Creating window...")

-- Create Window
local Window = Fluent:CreateWindow({
    Title = "📍 script |  Get down",
    SubTitle = "boost",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Darker",
    MinimizeKey = Enum.KeyCode.LeftControl
})

-- Create Tabs
local Tabs = {
    Main = Window:AddTab({ Title = "🏠 Main", Icon = "home" }),
    Boost = Window:AddTab({ Title = "Boost Sender", Icon = "circle" }),
    Settings = Window:AddTab({ Title = "⚙️ Settings", Icon = "settings" })
}

-- MAIN TAB
Tabs.Main:AddParagraph({
    Title = "📍 Welcome to Fixz Hub!",
    Content = "Draw Perfect Circle - Auto score sender with adjustable frequency!"
})

local StatsLabel = Tabs.Main:AddParagraph({
    Title = "📊 Statistics",
    Content = "Calls Sent: 0 | Frequency: 10/s | Mode: Send"
})

Tabs.Main:AddButton({
    Title = "🎮 Join Discord",
    Description = "Get support and updates",
    Callback = function()
        setclipboard("https://discord.gg/CDyrbSY8Gx")
        Fluent:Notify({
            Title = "Discord Copied!",
            Content = "Discord invite copied to clipboard!",
            Duration = 3
        })
    end
})
Tabs.Boost:AddButton({
    Title = "boost 1",
    Description = "idk",
    Callback = function()
        local args = {
	1
}
game:GetService("ReplicatedStorage"):WaitForChild("ApplyBoost"):FireServer(unpack(args))
  end
})

Tabs.Boost:AddButton({
    Title = "invisible mine",
    Description = "idk",
    Callback = function()
        local args = {
	2
}
game:GetService("ReplicatedStorage"):WaitForChild("ApplyBoost"):FireServer(unpack(args))
  end
})

Tabs.Boost:AddButton({
    Title = "speed boost",
    Description = "idk",
    Callback = function()
        local args = {
	3
}
game:GetService("ReplicatedStorage"):WaitForChild("ApplyBoost"):FireServer(unpack(args))
  end
})

Tabs.Boost:AddButton({
    Title = "jump boost",
    Description = "idk",
    Callback = function()
        local args = {
	4
}
game:GetService("ReplicatedStorage"):WaitForChild("ApplyBoost"):FireServer(unpack(args))
  end
})

Tabs.Boost:AddButton({
    Title = "boost 5",
    Description = "idk",
    Callback = function()
        local args = {
	5
}
game:GetService("ReplicatedStorage"):WaitForChild("ApplyBoost"):FireServer(unpack(args))
  end
})

Tabs.Boost:AddButton({
    Title = "boost 6",
    Description = "idk",
    Callback = function()
        local args = {
	6
}
game:GetService("ReplicatedStorage"):WaitForChild("ApplyBoost"):FireServer(unpack(args))
  end
})

Tabs.Boost:AddButton({
    Title = "boost 7",
    Description = "idk",
    Callback = function()
        local args = {
	7
}
game:GetService("ReplicatedStorage"):WaitForChild("ApplyBoost"):FireServer(unpack(args))
  end
})

Tabs.Boost:AddButton({
    Title = "boost 8",
    Description = "idk",
    Callback = function()
        local args = {
	8
}
game:GetService("ReplicatedStorage"):WaitForChild("ApplyBoost"):FireServer(unpack(args))
  end
})

Tabs.Boost:AddButton({
    Title = "boost 9",
    Description = "idk",
    Callback = function()
        local args = {
	9
}
game:GetService("ReplicatedStorage"):WaitForChild("ApplyBoost"):FireServer(unpack(args))
  end
})

Tabs.Boost:AddButton({
    Title = "boost 10",
    Description = "idk",
    Callback = function()
        local args = {
	10
}
game:GetService("ReplicatedStorage"):WaitForChild("ApplyBoost"):FireServer(unpack(args))
  end
})

Tabs.Boost:AddButton({
    Title = "boost 11",
    Description = "idk",
    Callback = function()
        local args = {
	11
}
game:GetService("ReplicatedStorage"):WaitForChild("ApplyBoost"):FireServer(unpack(args))
  end
})

Tabs.Boost:AddButton({
    Title = "boost 12",
    Description = "idk",
    Callback = function()
        local args = {
	12
}
game:GetService("ReplicatedStorage"):WaitForChild("ApplyBoost"):FireServer(unpack(args))
  end
})

Tabs.Boost:AddButton({
    Title = "troll",
    Description = "idk",
    Callback = function()
 local Library = loadstring(Game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wizard"))()

local PhantomForcesWindow = Library:NewWindow("Get.Down")

local KillingCheats = PhantomForcesWindow:NewSection("main")
KillingCheats:CreateButton("kill", function()
-- 💀 Revenge Executor GUI
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- 🧱 GUI chính
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RevengeGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 300, 0, 350)
Frame.Position = UDim2.new(0.5, -150, 0.5, -175)
Frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Frame.BackgroundTransparency = 0.1
Frame.Active = true
Frame.Draggable = true
Frame.Parent = ScreenGui

-- 🧾 Thanh tiêu đề
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
Title.Text = "💀 Revenge Executor"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 20
Title.Font = Enum.Font.SourceSansBold
Title.Parent = Frame

-- ❌ Nút X (đóng GUI)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -35, 0, 5)
CloseBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.TextSize = 18
CloseBtn.Parent = Frame

CloseBtn.MouseButton1Click:Connect(function()
	ScreenGui:Destroy()
end)

-- 📜 Danh sách người chơi
local ScrollingFrame = Instance.new("ScrollingFrame")
ScrollingFrame.Size = UDim2.new(1, 0, 1, -40)
ScrollingFrame.Position = UDim2.new(0, 0, 0, 40)
ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
ScrollingFrame.ScrollBarThickness = 6
ScrollingFrame.BackgroundTransparency = 1
ScrollingFrame.Parent = Frame

-- 🧩 Tạo nút cho từng player
local function CreatePlayerButton(player)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(1, -10, 0, 30)
	btn.Position = UDim2.new(0, 5, 0, #ScrollingFrame:GetChildren() * 35)
	btn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
	btn.Text = "⚔️ " .. player.Name
	btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	btn.TextSize = 18
	btn.Font = Enum.Font.SourceSansBold
	btn.Parent = ScrollingFrame

	btn.MouseButton1Click:Connect(function()
		local ev = ReplicatedStorage:FindFirstChild("RevengeEvent")
		if ev then
			pcall(function()
				ev:FireServer(player.Name)
				game.StarterGui:SetCore("SendNotification", {
					Title = "Revenge Sent",
					Text = "💀 Đã revenge " .. player.Name,
					Duration = 3
				})
			end)
		else
			warn("Không tìm thấy sự kiện RevengeEvent trong ReplicatedStorage")
		end
	end)
end

-- 🔁 Cập nhật danh sách
local function RefreshPlayerList()
	for _, obj in ipairs(ScrollingFrame:GetChildren()) do
		if obj:IsA("TextButton") then obj:Destroy() end
	end

	for _, plr in ipairs(Players:GetPlayers()) do
		if plr ~= LocalPlayer then
			CreatePlayerButton(plr)
		end
	end

	ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, #Players:GetPlayers() * 35)
end

Players.PlayerAdded:Connect(RefreshPlayerList)
Players.PlayerRemoving:Connect(RefreshPlayerList)
RefreshPlayerList()

print("HI")
end)

KillingCheats:CreateButton("Give Bomb", function()
-- 💣 Player List Give Bomb GUI
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- 🧱 GUI chính
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GiveBombGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 300, 0, 350)
Frame.Position = UDim2.new(0.5, -150, 0.5, -175)
Frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Frame.BackgroundTransparency = 0.1
Frame.Active = true
Frame.Draggable = true
Frame.Parent = ScreenGui

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
Title.Text = "💣 Give Bomb Executor"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 20
Title.Font = Enum.Font.SourceSansBold
Title.Parent = Frame

-- ❌ Nút X (đóng GUI)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -35, 0, 5)
CloseBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.TextSize = 18
CloseBtn.Parent = Frame

CloseBtn.MouseButton1Click:Connect(function()
	ScreenGui:Destroy()
end)

-- 🧾 Danh sách cuộn
local ScrollingFrame = Instance.new("ScrollingFrame")
ScrollingFrame.Size = UDim2.new(1, 0, 1, -40)
ScrollingFrame.Position = UDim2.new(0, 0, 0, 40)
ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
ScrollingFrame.ScrollBarThickness = 6
ScrollingFrame.BackgroundTransparency = 1
ScrollingFrame.Parent = Frame

-- 🧩 Hàm tạo nút cho từng player
local function CreatePlayerButton(player)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(1, -10, 0, 30)
	btn.Position = UDim2.new(0, 5, 0, #ScrollingFrame:GetChildren() * 35)
	btn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
	btn.Text = "💥 " .. player.Name
	btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	btn.TextSize = 18
	btn.Font = Enum.Font.SourceSansBold
	btn.Parent = ScrollingFrame

	btn.MouseButton1Click:Connect(function()
		local ev = ReplicatedStorage:FindFirstChild("GiveBomb")
		if ev then
			pcall(function()
				ev:FireServer(player)
				game.StarterGui:SetCore("SendNotification", {
					Title = "GiveBomb";
					Text = "Đã gửi bomb cho " .. player.Name;
					Duration = 3;
				})
			end)
		else
			warn("Không tìm thấy sự kiện GiveBomb trong ReplicatedStorage")
		end
	end)
end

-- 🧠 Hàm cập nhật danh sách
local function RefreshPlayerList()
	for _, obj in ipairs(ScrollingFrame:GetChildren()) do
		if obj:IsA("TextButton") then obj:Destroy() end
	end

	for _, plr in ipairs(Players:GetPlayers()) do
		if plr ~= LocalPlayer then
			CreatePlayerButton(plr)
		end
	end

	ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, #Players:GetPlayers() * 35)
end

-- ⚙️ Cập nhật khi có người vào/ra
Players.PlayerAdded:Connect(RefreshPlayerList)
Players.PlayerRemoving:Connect(RefreshPlayerList)

RefreshPlayerList()

print("HI")
end)

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local remoteName = "SlipEvent" -- đổi tên tại đây

local remote = ReplicatedStorage:FindFirstChild(remoteName)
if remote and remote:IsA("RemoteEvent") then
    remote:Destroy()
    print("you not fall:", remoteName)
else
    warn("❌ Không tìm thấy RemoteEvent:", remoteName)
end

local autoPlantBomb = false

KillingCheats:CreateToggle("Auto Plant Bomb", function(state)
    autoPlantBomb = state
    print("Auto Plant Bomb:", state)

    while autoPlantBomb do
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local plantEvent = ReplicatedStorage:WaitForChild("PlantBomb")

        if plantEvent then
            plantEvent:FireServer()
            print("Planted Bomb!")
        else
            warn("PlantBomb event not found!")
        end

        task.wait(0.5)
    end
end)
  end
})

-- SETTINGS TAB
SaveManager:SetLibrary(Fluent)
InterfaceManager:SetLibrary(Fluent)

SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({})

InterfaceManager:SetFolder("FixzHub")
SaveManager:SetFolder("FixzHub/DrawPerfectCircle")

InterfaceManager:BuildInterfaceSection(Tabs.Settings)
SaveManager:BuildConfigSection(Tabs.Settings)

Window:SelectTab(1)

Fluent:Notify({
    Title = "script Loaded!",
    Content = "Fr!",
    Duration = 5
})

SaveManager:LoadAutoloadConfig()

print("script fully loaded!")
print(" Use the Score Sender tab to configure and start!")
