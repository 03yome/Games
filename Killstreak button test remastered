local Library = loadstring(Game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wizard"))()

local PhantomForcesWindow = Library:NewWindow("Killstreak button test remastered")

local KillingCheats = PhantomForcesWindow:NewSection("main")
KillingCheats:CreateButton("Aura Hacker", function()
-- GUI

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "hack"          -- đặt tên tùy ý
ScreenGui.ResetOnSpawn = false          -- không mất khi respawn
ScreenGui.Parent = game:GetService("CoreGui")

local ToggleBtn = Instance.new("TextButton", ScreenGui)
ToggleBtn.Size = UDim2.new(0,150,0,40)
ToggleBtn.Position = UDim2.new(0.1,0,0.2,0)
ToggleBtn.Text = "TheForce: OFF"
ToggleBtn.BackgroundColor3 = Color3.fromRGB(50,50,50)
ToggleBtn.TextColor3 = Color3.fromRGB(255,255,255)
ToggleBtn.Active = true
ToggleBtn.Draggable = true

local Auto = false
ToggleBtn.MouseButton1Click:Connect(function()
    Auto = not Auto
    ToggleBtn.Text = Auto and "Aura: ON" or "Aura: OFF"
    ToggleBtn.BackgroundColor3 = Auto and Color3.fromRGB(0,170,0) or Color3.fromRGB(50,50,50)
end)

-- Auto loop
local remote = game:GetService("ReplicatedStorage"):WaitForChild("TheForce")
task.spawn(function()
    while wait(1) do
        if Auto then
            pcall(function()
                remote:FireServer()
            end)
        end
    end
end)

print("HI")
end)

KillingCheats:CreateButton("Haki Hacker", function()
game:GetService("ReplicatedStorage"):WaitForChild("KillstreakEvents"):WaitForChild("Haki"):FireServer()
print("HI")
end)

KillingCheats:CreateButton("Aura Hacker.v2", function()
local RS = game:GetService("ReplicatedStorage")
local remote = RS:WaitForChild("TheForce2")
local Player = game.Players.LocalPlayer

-- GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "hack"
ScreenGui.Parent = Player:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false  -- ✅ GUI sẽ không mất khi nhân vật chết

local ToggleBtn = Instance.new("TextButton", ScreenGui)
ToggleBtn.Size = UDim2.new(0,150,0,40)
ToggleBtn.Position = UDim2.new(0.1,0,0.2,0)
ToggleBtn.Text = "TheForce: OFF"
ToggleBtn.BackgroundColor3 = Color3.fromRGB(50,50,50)
ToggleBtn.TextColor3 = Color3.fromRGB(255,255,255)
ToggleBtn.Draggable = true

local Auto = false
ToggleBtn.MouseButton1Click:Connect(function()
    Auto = not Auto
    ToggleBtn.Text = Auto and "Aura: ON" or "Aura: OFF"
    ToggleBtn.BackgroundColor3 = Auto and Color3.fromRGB(0,170,0) or Color3.fromRGB(50,50,50)
end)

-- Auto loop
task.spawn(function()
    while wait(1) do
        if Auto then
            pcall(function()
                remote:FireServer()
            end)
        end
    end
end)

print("HI")
end)

KillingCheats:CreateButton("Orb Hacker", function()
game:GetService("ReplicatedStorage"):WaitForChild("AbilityEvents"):WaitForChild("OrbEvent"):FireServer()
print("HI")
end)

KillingCheats:CreateButton("Orb sans Hacker", function()
game:GetService("ReplicatedStorage"):WaitForChild("AbilityEvents"):WaitForChild("ORBAbility"):FireServer()
print("HI")
end)

KillingCheats:CreateButton("Power up Hacker", function()
game:GetService("ReplicatedStorage"):WaitForChild("AbilityEvents"):WaitForChild("Powerup"):FireServer()
print("HI")
end)

KillingCheats:CreateButton("autokill", function()
local Players = game:GetService("Players")
local RS = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "hack"
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

-- Container draggable
local Container = Instance.new("Frame", ScreenGui)
Container.Size = UDim2.new(0, 200, 0, 180)
Container.Position = UDim2.new(0.1, 0, 0.2, 0)
Container.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
Container.Active = true
Container.Draggable = true

-- Toggle Button
local ToggleBtn = Instance.new("TextButton", Container)
ToggleBtn.Size = UDim2.new(1, 0, 0, 40)
ToggleBtn.Position = UDim2.new(0, 0, 0, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Text = "Auto Kill: OFF"

-- Dropdown label
local Label = Instance.new("TextLabel", Container)
Label.Size = UDim2.new(1, 0, 0, 20)
Label.Position = UDim2.new(0, 0, 0, 45)
Label.BackgroundColor3 = Color3.fromRGB(50,50,50)
Label.TextColor3 = Color3.fromRGB(255,255,255)
Label.Text = "Select Part:"

-- Dropdown buttons
local parts = {"Left Arm", "Right Arm", "HumanoidRootPart", "Head", "Torso"}
local selectedPart = "Left Arm"
for i, partName in ipairs(parts) do
    local btn = Instance.new("TextButton", Container)
    btn.Size = UDim2.new(1, 0, 0, 20)
    btn.Position = UDim2.new(0, 0, 0, 65 + 20*(i-1))
    btn.BackgroundColor3 = Color3.fromRGB(70,70,70)
    btn.TextColor3 = Color3.fromRGB(255,255,255)
    btn.Text = partName
    btn.MouseButton1Click:Connect(function()
        selectedPart = partName
        print("✅ Selected Part: "..selectedPart)
    end)
end

-- Toggle AutoKill
local AutoKill = false
ToggleBtn.MouseButton1Click:Connect(function()
    AutoKill = not AutoKill
    ToggleBtn.Text = AutoKill and "Auto Kill: ON" or "Auto Kill: OFF"
    ToggleBtn.BackgroundColor3 = AutoKill and Color3.fromRGB(0,170,0) or Color3.fromRGB(30,30,30)
end)

-- Auto Kill Loop
local remote = RS:WaitForChild("SlapRemotes"):WaitForChild("KillstreakHit")
task.spawn(function()
    while task.wait(0.1) do
        if AutoKill then
            for _, plr in ipairs(Players:GetPlayers()) do
                if plr ~= LocalPlayer and plr.Character then
                    local part = plr.Character:FindFirstChild(selectedPart)
                    if part then
                        pcall(function()
                            remote:FireServer(part)
                        end)
                    end
                end
            end
        end
    end
end)
local Players = game:GetService("Players")
local RS = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "hack"
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

-- Container draggable
local Container = Instance.new("Frame", ScreenGui)
Container.Size = UDim2.new(0, 200, 0, 180)
Container.Position = UDim2.new(0.1, 0, 0.2, 0)
Container.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
Container.Active = true
Container.Draggable = true

-- Toggle Button
local ToggleBtn = Instance.new("TextButton", Container)
ToggleBtn.Size = UDim2.new(1, 0, 0, 40)
ToggleBtn.Position = UDim2.new(0, 0, 0, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Text = "Auto Kill: OFF"

-- Dropdown label
local Label = Instance.new("TextLabel", Container)
Label.Size = UDim2.new(1, 0, 0, 20)
Label.Position = UDim2.new(0, 0, 0, 45)
Label.BackgroundColor3 = Color3.fromRGB(50,50,50)
Label.TextColor3 = Color3.fromRGB(255,255,255)
Label.Text = "Select Part:"

-- Dropdown buttons
local parts = {"Left Arm", "Right Arm", "HumanoidRootPart", "Head", "Torso"}
local selectedPart = "Left Arm"
for i, partName in ipairs(parts) do
    local btn = Instance.new("TextButton", Container)
    btn.Size = UDim2.new(1, 0, 0, 20)
    btn.Position = UDim2.new(0, 0, 0, 65 + 20*(i-1))
    btn.BackgroundColor3 = Color3.fromRGB(70,70,70)
    btn.TextColor3 = Color3.fromRGB(255,255,255)
    btn.Text = partName
    btn.MouseButton1Click:Connect(function()
        selectedPart = partName
        print("✅ Selected Part: "..selectedPart)
    end)
end

-- Toggle AutoKill
local AutoKill = false
ToggleBtn.MouseButton1Click:Connect(function()
    AutoKill = not AutoKill
    ToggleBtn.Text = AutoKill and "Auto Kill: ON" or "Auto Kill: OFF"
    ToggleBtn.BackgroundColor3 = AutoKill and Color3.fromRGB(0,170,0) or Color3.fromRGB(30,30,30)
end)

-- Auto Kill Loop
local remote = RS:WaitForChild("SlapRemotes"):WaitForChild("KillstreakHit")
task.spawn(function()
    while task.wait(0.1) do
        if AutoKill then
            for _, plr in ipairs(Players:GetPlayers()) do
                if plr ~= LocalPlayer and plr.Character then
                    local part = plr.Character:FindFirstChild(selectedPart)
                    if part then
                        pcall(function()
                            remote:FireServer(part)
                        end)
                    end
                end
            end
        end
    end
end)

print("HI")
end)

KillingCheats:CreateButton("give kill", function()
local RS = game:GetService("ReplicatedStorage")
local remote = RS:WaitForChild("AdminEvents"):WaitForChild("GiveKill")

-- 📌 Tạo GUI
local RS = game:GetService("ReplicatedStorage")
local remote = RS:WaitForChild("AdminEvents"):WaitForChild("GiveKill")
local Players = game:GetService("Players")
local Player = Players.LocalPlayer

-- 📌 Tạo GUI
local gui = Instance.new("ScreenGui")
gui.Name = "hack"
gui.ResetOnSpawn = false
gui.Parent = game.CoreGui

-- 📌 Tạo Toggle Button
local ToggleBtn = Instance.new("TextButton", gui)
ToggleBtn.Size = UDim2.new(0, 150, 0, 40)
ToggleBtn.Position = UDim2.new(0.1, 0, 0.2, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Text = "GiveKill: OFF"
ToggleBtn.Active = true
ToggleBtn.Draggable = true

-- 📌 Trạng thái auto loop
local AutoLoop = false

-- 🔘 Bấm để đổi trạng thái
ToggleBtn.MouseButton1Click:Connect(function()
    AutoLoop = not AutoLoop
    ToggleBtn.Text = AutoLoop and "GiveKill: ON" or "GiveKill: OFF"
    ToggleBtn.BackgroundColor3 = AutoLoop and Color3.fromRGB(0,170,0) or Color3.fromRGB(30,30,30)
end)

-- 🔥 LOOP GIVEKILL
task.spawn(function()
    while true do
        if AutoLoop then
            pcall(function()
                remote:FireServer()
            end)
        end
        wait(0.1) -- tốc độ spam
    end
end)


print("HI")
end)

KillingCheats:CreateButton("autokill OG", function()
local plr = game.Players.LocalPlayer
local rs = game:GetService("ReplicatedStorage")
local slapFolder = rs:WaitForChild("SlapRemotes")

-- GUI chính
local gui = Instance.new("ScreenGui")
gui.Name = "hack"
gui.ResetOnSpawn = false
gui.Parent = game.CoreGui

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0, 250, 0, 150)
frame.Position = UDim2.new(0.4,0,0.3,0)
frame.BackgroundColor3 = Color3.fromRGB(40,40,40)
frame.Active = true
frame.Draggable = true

-- Title
local title = Instance.new("TextLabel", frame)
title.Size = UDim2.new(1,0,0,25)
title.BackgroundColor3 = Color3.fromRGB(30,30,30)
title.Text = "Auto Kill Menu"
title.TextColor3 = Color3.new(1,1,1)
title.Font = Enum.Font.SourceSansBold
title.TextSize = 18

-- Dropdown button
local dropdown = Instance.new("TextButton", frame)
dropdown.Size = UDim2.new(1,-20,0,30)
dropdown.Position = UDim2.new(0,10,0,35)
dropdown.BackgroundColor3 = Color3.fromRGB(60,60,60)
dropdown.TextColor3 = Color3.new(1,1,1)
dropdown.Text = "Chọn Kill Remote"

-- Scrollable list
local listFrame = Instance.new("ScrollingFrame", frame)
listFrame.Size = UDim2.new(1,-20,0,80)
listFrame.Position = UDim2.new(0,10,0,70)
listFrame.BackgroundColor3 = Color3.fromRGB(25,25,25)
listFrame.Visible = false
listFrame.ScrollBarThickness = 6
listFrame.CanvasSize = UDim2.new(0,0,0,0)
listFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y

local UIList = Instance.new("UIListLayout", listFrame)
UIList.SortOrder = Enum.SortOrder.LayoutOrder
UIList.Padding = UDim.new(0,2)

-- Tạo nút remote
local chosenRemote = nil
for _, remote in ipairs(slapFolder:GetChildren()) do
    if remote:IsA("RemoteEvent") then
        local btn = Instance.new("TextButton", listFrame)
        btn.Size = UDim2.new(1,0,0,30)
        btn.BackgroundColor3 = Color3.fromRGB(50,50,50)
        btn.TextColor3 = Color3.new(1,1,1)
        btn.Font = Enum.Font.SourceSans
        btn.TextSize = 18
        btn.TextWrapped = true -- wrap text dài
        btn.TextXAlignment = Enum.TextXAlignment.Left
        btn.Text = remote.Name

        btn.MouseButton1Click:Connect(function()
            dropdown.Text = "Remote: "..remote.Name
            chosenRemote = remote
            listFrame.Visible = false
        end)
    end
end

-- Hiện/ẩn list
dropdown.MouseButton1Click:Connect(function()
    listFrame.Visible = not listFrame.Visible
end)

-- Auto Kill toggle
local toggle = Instance.new("TextButton", frame)
toggle.Size = UDim2.new(1,-20,0,30)
toggle.Position = UDim2.new(0,10,0,110)
toggle.BackgroundColor3 = Color3.fromRGB(100,30,30)
toggle.TextColor3 = Color3.new(1,1,1)
toggle.Text = "Auto Kill: OFF"

local running = false
toggle.MouseButton1Click:Connect(function()
    running = not running
    toggle.Text = running and "Auto Kill: ON" or "Auto Kill: OFF"
    toggle.BackgroundColor3 = running and Color3.fromRGB(30,120,30) or Color3.fromRGB(100,30,30)

    if running then
        task.spawn(function()
            while running do
                if chosenRemote then
                    for _, pl in ipairs(game.Players:GetPlayers()) do
                        if pl ~= plr and pl.Character then
                            local hum = pl.Character:FindFirstChild("Humanoid")
                            local hrp = pl.Character:FindFirstChild("HumanoidRootPart")
                            if hum and hrp then
                                pcall(function()
                                    chosenRemote:FireServer(hum, hrp.Position, 1)
                                end)
                            end
                        end
                    end
                end
                task.wait(0.15)
            end
        end)
    end
end)

print("HI")
end)

KillingCheats:CreateButton("next", function()

local Library = loadstring(Game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wizard"))()

local PhantomForcesWindow = Library:NewWindow("Frame 2")

local KillingCheats = PhantomForcesWindow:NewSection("main")
KillingCheats:CreateButton("BeamOfDeath", function()
game:GetService("ReplicatedStorage"):WaitForChild("AbilityEvents"):WaitForChild("BeamOfDeath"):FireServer()
print("HI")
end)

KillingCheats:CreateButton("OrbEventEx", function()
game:GetService("ReplicatedStorage"):WaitForChild("AbilityEvents"):WaitForChild("OrbEventEx"):FireServer()
print("HI")
end)

KillingCheats:CreateButton("delete button", function()
local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local CoreGui = game:GetService("CoreGui")

local guiName = "hack" -- <-- đổi tên GUI cần xóa

-- Xóa trong PlayerGui
local playerGui = Player:FindFirstChild("PlayerGui")
if playerGui then
    local gui = playerGui:FindFirstChild(guiName)
    if gui then
        gui:Destroy()
        print("✅ Đã xóa "..guiName.." trong PlayerGui")
    end
end

-- Xóa trong CoreGui
local coreGui = CoreGui:FindFirstChild(guiName)
if coreGui then
    coreGui:Destroy()
    print("✅ Đã xóa "..guiName.." trong CoreGui")
end

print("HI")
end)

KillingCheats:CreateButton("funny", function()
game:GetService("ReplicatedStorage"):WaitForChild("AbilityEvents"):WaitForChild("Duel"):FireServer()
print("HI")
end)

KillingCheats:CreateButton("HallowJack", function()
game:GetService("ReplicatedStorage"):WaitForChild("AbilityEvents"):WaitForChild("HallowJack"):FireServer()
print("HI")
end)

print("HI")
end)
